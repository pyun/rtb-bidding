请帮我生成一个RTB应用，包括客户端和服务器端,具体要求如下：
1. 客户端按照rtb协议发送请求，服务器端接收请求并返回响应;
2. rtb协议为json格式，格式如rtb.json文件；
3. 客户端在向服务器端发送rtb请求时，按照rtb.json的格式模拟一些数据，模拟方式如下：
    1. 生成一个随机的uuid，作为request_id;
    2. 生成一个随机的uuid，作为imp_id;
    3. 生成一个随机的uuid，作为banner_id;
    4. 生成一个随机的uuid，作为device_id;
    5. 生成一个随机的uuid，作为user_id;
    6. 生成一个随机的uuid，作为app_id;
    7. 生成一个随机的uuid，作为site_id;
    8. 生成一个随机的uuid，作为publisher_id;
    9. 针对操作系统，随机在[iOS,Android,Web]中选择一个;
    10. 针对设备国家，随机在[US,CA,CN,UA,JP,SIN]中选择一个；
4. 客户端可提供两个参数，一个是并发请求数，一个是持续时间，如果设置为0，表示一直运行不停；
5. 服务器端在收到客户端的竞价请求后，做如下处理：
    1. 首先打印控制台日志，打印客户端发来的竞价请求json；
    2. 按照配置的比例，按照比例返回bid和no-bid请求；
    3. 请根据行业知识，构建bid和no-bid的字段内容；
5. 请用python语言实现，将客户端和服务器端做成统一的一个项目，提供一个客户端启动命令和服务器端启动命令。
6. 这是一个测试项目，实现尽量简单，但需要满足如下要求：
    1. 客户端能够提供一定的qps压测能力，采用多线程方式实现较高的并发访问能力；
    2. 服务器端采用无状态设计，支持在EKS中部署